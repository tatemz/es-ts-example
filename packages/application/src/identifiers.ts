import * as Domain from "@es-ts-example/domain";

export const CounterId = Domain.CounterId;
export type CounterId = Domain.CounterId;

export const UserId = Domain.UserId;
export type UserId = Domain.UserId;

export const ArticleId = Domain.ArticleId;
export type ArticleId = Domain.ArticleId;
